#include <stdio.h>
int main ();
{
    float Ventas=0, sumaVentas=0;
    do {
        printf ("Ingrese la venta:\n");
        scanf ("%f",&Venta);
        Ventas += sumaVentas;
    }while (ventas !=0);
    printf ("La venta total es: %f",&sumaVentas);

    return 0;
}